# TODO: Download Merge Skill

Design and implement a `myteam` skill that guides an agent through merging updated remote content with a dirty local copy.

This should cover:

- detecting when downloaded content has local modifications
- presenting the difference between local and updated remote content
- guiding an agent through merge or conflict resolution steps
- defining when the skill should be invoked during `myteam update`

This document is intentionally a placeholder until the skill design is specified.
