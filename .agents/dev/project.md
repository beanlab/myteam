# About the `myteam` project

`myteam` is a Python framework + CLI for building and using agent harnesses.

It is in early development with a small user base. Breaking changes are OK, especially when they move the API and behavior towards a more intuitive, capable, extensible, durable design. 

It seeks to be minimal in its approach wherever possible. This improves maintainability as well as user comprehension.